{
  "name": "artificial-mohadith",
  "version": "0.0.1",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=browser --target=es2019 --format=cjs --outfile=main.js --external:obsidian",
    "postbuild": "cp manifest.json main.js README.md LICENSE $artificial_mohadith_dist",
    "dev": "esbuild src/main.ts --bundle --platform=browser --target=es2019 --format=cjs --outfile=main.js --external:obsidian --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@google/genai": "^1.19.0",
    "obsidian": "^1.8.7"
  }
}
